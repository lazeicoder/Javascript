// Gives the whole document object
console.log(document);

// Gives the head object in the document
console.log(document.head);

// Gives the title object of the document
console.log(document.title);

// Gives the body object of the document
console.log(document.body);